name: rna-transcription
version: 1.2.0.7

dependencies:
  - base

library:
  exposed-modules: DNA
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - rna-transcription
      - hspec
